<%= form_with(model:@post,local:true,url:coose_new_or_edit) do |f| %>


<p><%= image_tag(@post.image.url)if @post.image&&@post.image.url %></p>
<p><%= f.file_field :image %></p>
<p><%= f.hidden_field :image_cache %></p>

<%= f.label:title %>
<%= f.text_field:title %>

<%= f.label:content %>
<%= f.text_field:content %>

<%= f.submit "投稿" %>

<% end %>
